<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record model="ir.actions.act_window" id="action_account_comment_template">
        <field name="name">Account Comments</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">base.comment.template</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[("model_ids", "=", "account.move")]</field>
        <field name="context" eval="{'default_models': 'account.move'}" />
    </record>

    <menuitem
        name="Document Comments"
        id="menu_base_comment_template_invoice"
        action="action_account_comment_template"
        parent="account.account_management_menu"
    />

</odoo>
